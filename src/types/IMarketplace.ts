import { ICollection } from "./ICollection";

export interface IMarketplaces {
  featuredDrops: ICollection[];
  headerDrops: ICollection[];
  newDrops: ICollection[];
  spotlightDrops: ICollection[];
}
